#Los rangos son parecidos a los vectores en c++ , solo que me permite recorrer sus valores como yo quiera. EJ:

r=range(1,10,1) #ACa va a ir de los valores 1 al 10, de 1 en 1.
print(r) #Aca solo va a mostrar que es un rango y  entre que valores esta, pero no los valores dentro del rango

#para ver los valores del rango usamos la funcionn list
print(list(r))

r=range(1,10,2) #Aaca va a ir de 1 al 10, pero de 2 en 2.
print(list(r))